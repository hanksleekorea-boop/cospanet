# 증거 색인

- `OWN-*`: 프로젝트 파일·계획·구현
- `TEST-*`: 이번 실행 또는 반복 가능한 로컬 검사
- `PUBLIC-*`: 외부 공개 URL·공식 자료
- `HIST-*`: 과거 실행 결과. 현재 증거로 승격하지 않음
- `UNKNOWN-*`: 확인 실패·외부 권한 부족

모든 점수·계획·완료 표현은 위 등급을 분리한다. 계획 카드가 `DONE`인 것과 실제 운영이 `DONE-O`인 것은 동일하지 않다.
