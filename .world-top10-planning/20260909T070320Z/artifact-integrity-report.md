# 산출물 무결성 보고

## 파일 범위

v3.2 새 산출물은 이 실행 폴더에만 기록했다. 제품 코드, 배포 설정, Git 기록, 외부 계정은 변경하지 않았다.

현재 실행 폴더 집계: 78개 파일, 981줄, 65,661자. 이 수치는 계획·증거 구조의 분량이며 제품 완성도나 10배 계약 달성률이 아니다.

## 검사

| 검사 | 결과 |
|---|---|
| JSON 25개 파싱 | PASS |
| JSONL 20개 파싱 | PASS |
| CSV 헤더·행 구조 | PASS |
| ID 중복(신규 레코드) | PASS |
| 기존 파일 덮어쓰기 | PASS (0건) |
| 비밀값 원문 포함 | PASS (확인 가능한 신규 파일 없음) |
| `npm run verify:project-history` | PASS (기존 44/44) |
| `git diff --check` | PASS (줄바꿈 경고만) |
| 외부 쓰기·배포 | 0건 |

산출물의 구조 검사는 실제 제품 기능·OAuth·D1·DNS·Android 합격을 의미하지 않는다.
